import { Test, TestingModule } from '@nestjs/testing';

import { MarkdownService } from '../markdown.service';
import { YjsDocReaderService } from '../../collaboration/yjs-doc-reader.service';
import type { YjsWidgetData } from '../../collaboration/types/yjs-widget.types';
import {
  SECTION_BUILDERS,
  TechStackBuilder,
  NamingConventionBuilder,
  FormatBuilder,
  GitConventionBuilder,
  CollaborationBuilder,
  CommunicationBuilder,
} from '../builders';

describe('MarkdownService', () => {
  let service: MarkdownService;
  let yjsDocReaderMock: { getWidgets: jest.Mock };
  const workspaceId = 'w1';

  beforeEach(async () => {
    yjsDocReaderMock = {
      getWidgets: jest.fn(),
    };

    const module: TestingModule = await Test.createTestingModule({
      providers: [
        MarkdownService,
        {
          provide: YjsDocReaderService,
          useValue: yjsDocReaderMock,
        },
        TechStackBuilder,
        NamingConventionBuilder,
        FormatBuilder,
        GitConventionBuilder,
        CollaborationBuilder,
        CommunicationBuilder,
        {
          provide: SECTION_BUILDERS,
          useFactory: (
            techStack: TechStackBuilder,
            naming: NamingConventionBuilder,
            format: FormatBuilder,
            git: GitConventionBuilder,
            collaboration: CollaborationBuilder,
            communication: CommunicationBuilder,
          ) => [techStack, naming, format, git, collaboration, communication],
          inject: [
            TechStackBuilder,
            NamingConventionBuilder,
            FormatBuilder,
            GitConventionBuilder,
            CollaborationBuilder,
            CommunicationBuilder,
          ],
        },
      ],
    }).compile();

    service = module.get<MarkdownService>(MarkdownService);
    jest.useFakeTimers().setSystemTime(new Date('2024-01-01T12:00:00Z'));
  });

  afterEach(() => {
    jest.useRealTimers();
  });

  describe('generateMarkdown', () => {
    it('위젯이 없으면 빈 상태 안내 메시지를 반환해야 한다', () => {
      yjsDocReaderMock.getWidgets.mockReturnValue([]);

      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('### 🚀 아직 작성된 내용이 없습니다.');
      expect(markdown).toContain('위젯을 추가하여 팀의 규칙을 정의해보세요!');
      expect(markdown).toContain('*Generated by team.config*');
    });
  });

  describe('TechStackBuilder', () => {
    it('기술 스택 위젯이 있으면 마크다운을 생성해야 한다', () => {
      const techStackWidget: YjsWidgetData = {
        widgetId: 'tech-1',
        type: 'TECH_STACK',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          subject: {
            selectedId: 's1',
            options: { s1: { value: 'Frontend', createdAt: 0 } },
          },
          techItems: [
            { id: 't1', name: 'React', category: 'Library' },
            { id: 't2', name: 'TypeScript', category: 'Language' },
          ],
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([techStackWidget]);

      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 🛠 기술 스택');
      expect(markdown).toContain('### Frontend');
      expect(markdown).toContain('React');
      expect(markdown).toContain('TypeScript');
    });

    it('techItems가 비어있으면 subject 헤더가 생성되지 않아야 한다', () => {
      const emptyTechWidget: YjsWidgetData = {
        widgetId: 'tech-empty-1',
        type: 'TECH_STACK',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          subject: {
            selectedId: 's1',
            options: { s1: { value: 'Backend', createdAt: 0 } },
          },
          techItems: [],
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([emptyTechWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 🛠 기술 스택');
      expect(markdown).not.toContain('### Backend');
    });
  });

  describe('NamingConventionBuilder', () => {
    it('네이밍 컨벤션 위젯이 있으면 마크다운을 생성해야 한다', () => {
      const namingConventionWidget: YjsWidgetData = {
        widgetId: 'naming-1',
        type: 'NAMING_CONVENTION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          frontend: {
            variable: 'camelCase',
            function: 'camelCase',
            component: 'PascalCase',
            constant: 'UPPER_SNAKE_CASE',
          },
          backend: {
            variable: 'camelCase',
            function: 'camelCase',
            class: 'PascalCase',
            constant: 'UPPER_SNAKE_CASE',
          },
          database: {
            table: 'snake_case',
            column: 'snake_case',
            index: 'idx_prefix',
            constraint: 'pk_prefix',
          },
          common: {
            utility: 'camelCase',
            constant: 'UPPER_SNAKE_CASE',
            type: 'PascalCase',
            enum: 'UPPER_SNAKE_CASE',
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([namingConventionWidget]);

      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 📝 네이밍 컨벤션');
      expect(markdown).toContain('### Frontend');
      expect(markdown).toContain('camelCase');
      expect(markdown).toContain('PascalCase');
      expect(markdown).toContain('### Backend');
      expect(markdown).toContain('### Database');
      expect(markdown).toContain('snake_case');
      expect(markdown).toContain('### Common');
      expect(markdown).toContain('열거형');
    });

    it('일부 필드가 없으면 기본값(-)으로 표시해야 한다', () => {
      const partialNamingWidget: YjsWidgetData = {
        widgetId: 'naming-partial-1',
        type: 'NAMING_CONVENTION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          frontend: {
            variable: 'camelCase',
          },
          backend: {},
          database: {
            table: 'snake_case',
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([partialNamingWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 📝 네이밍 컨벤션');
      expect(markdown).toContain('camelCase');
      expect(markdown).toContain('| 함수 | - |');
      expect(markdown).toContain('| 컴포넌트 | - |');
      expect(markdown).toContain('| 클래스 | - |');
      expect(markdown).toContain('snake_case');
    });

    it('모든 카테고리가 비어있으면 전체 기본값(-)으로 표시해야 한다', () => {
      const emptyNamingWidget: YjsWidgetData = {
        widgetId: 'naming-empty-1',
        type: 'NAMING_CONVENTION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {} as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([emptyNamingWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 📝 네이밍 컨벤션');
      expect(markdown).toContain('| 변수 | - |');
      expect(markdown).toContain('| 테이블 | - |');
      expect(markdown).toContain('| 유틸리티 | - |');
      expect(markdown).toContain('| 열거형 | - |');
    });
  });

  describe('FormatBuilder', () => {
    it('코드 포맷 위젯이 있으면 마크다운을 생성해야 한다', () => {
      const formatWidget: YjsWidgetData = {
        widgetId: 'format-1',
        type: 'CODE_FORMAT',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          printWidth: 80,
          useTabs: false,
          tabWidth: 2,
          semi: true,
          singleQuote: true,
          jsxSingleQuote: false,
          bracketSpacing: true,
          trailingComma: 'all',
          arrowParens: 'always',
          singleAttributePerLine: true,
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([formatWidget]);

      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## ⚙️ 코드 포맷 (Code Format)');
      expect(markdown).toContain('줄 길이');
      expect(markdown).toContain('80');
      expect(markdown).toContain('탭 사용');
      expect(markdown).toContain('들여쓰기 폭');
      expect(markdown).toContain('세미콜론');
      expect(markdown).toContain('홑따옴표');
      expect(markdown).toContain('후행 쉼표');
      expect(markdown).toContain('all');
    });

    it('반대 옵션(useTabs: true, semi: false 등)이 올바르게 표시되야 한다', () => {
      const formatWidget: YjsWidgetData = {
        widgetId: 'format-alt-1',
        type: 'CODE_FORMAT',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          printWidth: 120,
          useTabs: true,
          tabWidth: 4,
          semi: false,
          singleQuote: false,
          jsxSingleQuote: true,
          bracketSpacing: false,
          trailingComma: 'none',
          arrowParens: 'avoid',
          singleAttributePerLine: false,
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([formatWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('탭');
      expect(markdown).toContain('생략');
      expect(markdown).toContain('쌍따옴표');
      expect(markdown).toContain('공백 없음');
      expect(markdown).toContain('미사용');
      expect(markdown).toContain('한 줄에 배치');
    });
  });

  describe('GitConventionBuilder', () => {
    it('GITHUB_FLOW 전략이 선택되면 해당 설명을 표시해야 한다', () => {
      const gitConventionWidget: YjsWidgetData = {
        widgetId: 'git-1',
        type: 'GIT_CONVENTION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          strategy: {
            selectedId: 's1',
            options: { s1: { value: 'GITHUB_FLOW', createdAt: 0 } },
          },
          branchRules: {
            mainBranch: 'main',
            developBranch: 'develop',
            prefixes: {
              selectedIds: ['p1', 'p2'],
              options: {
                p1: { value: 'feat', createdAt: 0 },
                p2: { value: 'fix', createdAt: 0 },
              },
            },
          },
          commitConvention: {
            commitTypes: {
              selectedIds: ['c1'],
              options: { c1: { value: 'chore', createdAt: 0 } },
            },
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([gitConventionWidget]);

      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 🐙 Git 컨벤션');
      expect(markdown).toContain('### 🧩 브랜치 전략');
      expect(markdown).toContain('GitHub Flow');
      expect(markdown).toContain('### 🌿 브랜치 규칙');
      expect(markdown).toContain('main');
      expect(markdown).toContain('develop');
      expect(markdown).toContain('`feat`, `fix`');
      expect(markdown).toContain('### 📝 커밋 컨벤션');
      expect(markdown).toContain('chore');
    });

    it('GIT_FLOW 전략이 선택되면 해당 설명을 표시해야 한다', () => {
      const gitFlowWidget: YjsWidgetData = {
        widgetId: 'git-flow-1',
        type: 'GIT_CONVENTION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          strategy: {
            selectedId: 's1',
            options: { s1: { value: 'GIT_FLOW', createdAt: 0 } },
          },
          branchRules: {
            mainBranch: 'main',
            developBranch: '',
            prefixes: { selectedIds: [], options: {} },
          },
          commitConvention: {
            commitTypes: { selectedIds: [], options: {} },
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([gitFlowWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('Git Flow');
      expect(markdown).toContain('main, develop, feature, release, hotfix');
    });

    it('TRUNK_BASED 전략이 선택되면 해당 설명을 표시해야 한다', () => {
      const trunkBasedWidget: YjsWidgetData = {
        widgetId: 'trunk-1',
        type: 'GIT_CONVENTION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          strategy: {
            selectedId: 's1',
            options: { s1: { value: 'TRUNK_BASED', createdAt: 0 } },
          },
          branchRules: {
            mainBranch: 'main',
            developBranch: '-',
            prefixes: { selectedIds: [], options: {} },
          },
          commitConvention: {
            commitTypes: { selectedIds: [], options: {} },
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([trunkBasedWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('Trunk Based');
      expect(markdown).toContain('짧은 주기로 빠르게 통합');
    });

    it('developBranch가 없으면 Develop 브랜치 행이 생성되지 않아야 한다', () => {
      const noDevelopWidget: YjsWidgetData = {
        widgetId: 'no-develop-1',
        type: 'GIT_CONVENTION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          strategy: {
            selectedId: 's1',
            options: { s1: { value: 'GITHUB_FLOW', createdAt: 0 } },
          },
          branchRules: {
            mainBranch: 'main',
            prefixes: { selectedIds: [], options: {} },
          },
          commitConvention: {
            commitTypes: { selectedIds: [], options: {} },
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([noDevelopWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).not.toContain('Develop 브랜치');
    });
  });

  describe('CollaborationBuilder', () => {
    it('협업 규칙 위젯이 있으면 마크다운을 생성해야 한다', () => {
      const collaborationWidget: YjsWidgetData = {
        widgetId: 'collab-1',
        type: 'COLLABORATION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          prRules: {
            activeVersion: {
              selectedId: 'v1',
              options: { v1: { value: 'Semantic Versioning', createdAt: 0 } },
            },
            activeStrategy: {
              selectedId: 's1',
              options: { s1: { value: 'Squash and Merge', createdAt: 0 } },
            },
            labelRules: {
              selectedIds: ['l1', 'l2'],
              options: {
                l1: { value: 'bug', createdAt: 0 },
                l2: { value: 'feature', createdAt: 0 },
              },
            },
          },
          reviewPolicy: {
            approves: 2,
            maxReviewHours: 24,
            blockMerge: true,
          },
          workflow: {
            platform: {
              selectedId: 'p1',
              options: { p1: { value: 'GitHub Projects', createdAt: 0 } },
            },
            cycleValue: 2,
            cycleUnit: '주',
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([collaborationWidget]);

      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 🤝 협업 규칙');
      expect(markdown).toContain('### 🔀 Pull Request 규칙');
      expect(markdown).toContain('Semantic Versioning');
      expect(markdown).toContain('Squash and Merge');
      expect(markdown).toContain('`bug`, `feature`');
      expect(markdown).toContain('### 🧐 리뷰 정책 (Review Policy)');
      expect(markdown).toContain('🛡️ **2명**');
      expect(markdown).toContain('⏰ **24h**');
      expect(markdown).toContain('🔴 **YES**');
      expect(markdown).toContain('### 🔄 워크플로우 (Workflow)');
      expect(markdown).toContain('GitHub Projects');
      expect(markdown).toContain('2주');
    });

    it('blockMerge가 false이면 No로 표시해야 한다', () => {
      const collabWidget: YjsWidgetData = {
        widgetId: 'collab-alt-1',
        type: 'COLLABORATION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          prRules: {
            activeVersion: { selectedId: '', options: {} },
            activeStrategy: { selectedId: '', options: {} },
            labelRules: { selectedIds: [], options: {} },
          },
          reviewPolicy: {
            approves: 1,
            maxReviewHours: 12,
            blockMerge: false,
          },
          workflow: {
            platform: { selectedId: '', options: {} },
            cycleValue: 1,
            cycleUnit: '주',
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([collabWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('⚪ No');
      expect(markdown).not.toContain('🔴 **YES**');
    });
  });

  describe('CommunicationBuilder', () => {
    it('커뮤니케이션 위젯이 있으면 마크다운을 생성해야 한다', () => {
      const communicationWidget: YjsWidgetData = {
        widgetId: 'comm-1',
        type: 'COMMUNICATION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          communication: {
            urgent: {
              selectedId: 'u1',
              options: { u1: { value: '전화', createdAt: 0 } },
            },
            sync: {
              selectedId: 's1',
              options: { s1: { value: 'Zoom', createdAt: 0 } },
            },
            async: {
              selectedId: 'a1',
              options: { a1: { value: 'Slack', createdAt: 0 } },
            },
            official: {
              selectedId: 'o1',
              options: { o1: { value: '이메일', createdAt: 0 } },
            },
          },
          sla: {
            responseTime: 24,
          },
          timeManagement: {
            coreTimeStart: '10:00',
            coreTimeEnd: '18:00',
          },
          meeting: {
            noMeetingDay: '수요일',
            feedbackStyle: '직접적',
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([communicationWidget]);

      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 💬 커뮤니케이션');
      expect(markdown).toContain('### 📢 채널 가이드');
      expect(markdown).toContain('전화');
      expect(markdown).toContain('Zoom');
      expect(markdown).toContain('Slack');
      expect(markdown).toContain('이메일');
      expect(markdown).toContain('### ⏳ 시간 관리 및 미팅');
      expect(markdown).toContain('코어 타임 (Core Time)');
      expect(markdown).toContain('`10:00` ~ `18:00`');
      expect(markdown).toContain('최대 응답 시간 (SLA)');
      expect(markdown).toContain('24h');
      expect(markdown).toContain('미팅 없는 날');
      expect(markdown).toContain('수요일');
      expect(markdown).toContain('피드백 스타일');
      expect(markdown).toContain('직접적');
    });

    it('채널이 설정되지 않으면 해당 채널 행이 생성되지 않아야 한다', () => {
      const commWidget: YjsWidgetData = {
        widgetId: 'comm-alt-1',
        type: 'COMMUNICATION',
        layout: { x: 0, y: 0, width: 300, height: 300 },
        createdAt: Date.now(),
        content: {
          communication: {
            urgent: { selectedId: '', options: {} },
            sync: { selectedId: '', options: {} },
            async: { selectedId: '', options: {} },
            official: { selectedId: '', options: {} },
          },
          sla: { responseTime: 48 },
          timeManagement: {
            coreTimeStart: '09:00',
            coreTimeEnd: '17:00',
          },
          meeting: {
            noMeetingDay: '금요일',
            feedbackStyle: '간접적',
          },
        } as Record<string, unknown>,
      };

      yjsDocReaderMock.getWidgets.mockReturnValue([commWidget]);
      const markdown = service.generateMarkdown(workspaceId);

      expect(markdown).toContain('## 💬 커뮤니케이션');
      expect(markdown).not.toContain('긴급 (Urgent)');
      expect(markdown).toContain('48h');
      expect(markdown).toContain('금요일');
    });
  });
});
