import type { GitConventionData } from '../types/gitConvention';
import { GIT_CONVENTION_PRESETS } from './presets';

export const INITIAL_GIT_CONVENTION_DATA: GitConventionData =
  GIT_CONVENTION_PRESETS.GITHUB_FLOW;
