#!/bin/sh

# package.json 변경 감지 시 자동 npm install
changed_files=$(git diff-tree -r --name-only --no-commit-id ORIG_HEAD HEAD)

if echo "$changed_files" | grep -qE "package(-lock)?\.json"; then
  echo "📦 package.json 변경 감지! npm install 실행 중..."
  npm install
  echo "✅ 의존성 설치 완료!"
fi